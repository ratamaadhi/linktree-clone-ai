#!/usr/bin/env bash

# Run Prettier
bun prettier --write .

# Run ESLint
bun lint --fix

# Run unit tests (only if test files exist, excluding node_modules and e2e)
if [ -n "$(find ./tests/unit -name '*.test.ts' -o -name '*.test.tsx' -type f 2>/dev/null)" ]; then
  bun test tests/unit
fi
